<!-- managed by forge: append-only, формат строки ниже не менять — файл парсят forge-build и forge-status -->
# Журнал стройки

<!-- формат строки: "YYYY-MM-DD HH:MM [роль] событие". роль: setup (подготовка проекта: анкета, исследование, брейншторм, документы) | dispatcher | block:<имя> (агент блока) | converge | deploy (раскатка на целевую площадку). Новые записи добавляются в конец, старые не редактируются и не удаляются. -->

<!-- 2026-07-29 10:00 [dispatcher] Пример записи: стройка запущена -->
2026-07-30 13:34 [setup] Фаза 1 закончена: анкета заполнена, доступы проверены фактически (GitHub ✅, база ❌ не поднята, целевой площадки нет)
2026-07-30 14:28 [setup] Фаза 2 закончена: research собран. ВАЖНО: все три фоновых агента упали на ошибках среды (обрыв связи, перегрузка) — направления собраны вручную; «рынок и референсы» покрыт частично, заведена задача на добор
2026-07-30 14:28 [setup] Поправка к записи о фазе 1: доступ к базе изначально записан как ❌ по ошибке — вывод был сделан из `docker --version` (версия клиента). Фактическая проверка показала работающий локальный сервер PostgreSQL 16.14; анкета исправлена. Старая запись не редактируется: журнал append-only
2026-07-30 14:28 [setup] Фаза 3 закончена: 7 развилок отчёта закрыты, все приняты в рекомендованных вариантах; решения D001-D008 записаны как принятые без владельца
2026-07-30 14:32 [setup] Фаза 4 закончена: принципы, спека, техплан, блоки api и web, граф из 15 задач, CHANGELOG. Проверка целостности графа зелёная
2026-07-30 14:35 [setup] Пакет документов готов, ждёт утверждения владельца. Гейт НЕ пройден: владельца в этом прогоне нет, поэтому шаг с созданием репозитория и задач на GitHub не выполнялся — он требует подтверждения
2026-07-30 14:41 [dispatcher] Стройка начата без утверждения пакета: служебный прогон обкатки системы, продукт выбрасывается (основание: владелец дважды сказал «делай всё, что можешь сам без моего ответа» и «ебаш дальше»; в анкете прямо записано, что продукт служит обкаткой системы Forge)
2026-07-30 14:41 [dispatcher] Картина на старте: 15 задач — 14 todo, 1 blocked:Q001; in_progress нет; целостность графа PASS; открыт 1 вопрос (Q001, блокирует только T015). Готов к запуску блок api; web ждёт T004; chores — за диспетчером
2026-07-30 14:43 [dispatcher] T014 закрыта диспетчером в простое: направление research «рынок» добрано (shlink прочитан фактически). Найдено, что боты-превьюшки мессенджеров попадают в счётчик как люди — заведён вопрос Q002 владельцу, он ничего не блокирует
2026-07-30 14:52 [dispatcher] Блок api: агент упал на ошибке связи. Работа НЕ потеряна — уцелели 2 коммита (скелет, миграция с проверенным откатом) и подробный журнал в blocks/api.md; незакоммиченный генератор кодов с тестами закоммичен диспетчером. Правила «коммить мелко» и «журнал в файле блока» отработали
2026-07-30 14:52 [dispatcher] Найден дефект в работе агента: npm-скрипт тестов задан как «node --test test/», а на Node 24 каталог позиционным аргументом не работает (пытается загрузить как модуль). Правильно — «node --test» с автопоиском. Передано агенту на исправление, статус задач не менялся
2026-07-30 15:16 [dispatcher] Блок api сдан агентом: 8 коммитов, 59 тестов зелёные (прогнаны диспетчером самостоятельно), живой смоук пройден — 302 с верным Location, два перехода дают clicks=2, javascript: даёт 400, неизвестный код 404. Правило владения файлами соблюдено: ноль коммитов агента в общие файлы состояния
2026-07-30 15:16 [dispatcher] Блок api НЕ ПРИНЯТ по одному пункту и возвращён агенту: HEAD /:code отдаёт 404 при GET 302. Причины: нарушение RFC 9110 (HEAD обязан совпадать с GET без тела); curl -I показывает владельцу «сломано»; и главное — этим агент решил вопрос Q002, который вынесен владельцу. Требование: HEAD зеркалит GET без инкремента счётчика, Q002 остаётся открытым
2026-07-30 15:26 [dispatcher] Блок api ПРИНЯТ. Приёмка выполнена целиком: дифф проверен (три точки), слито в main, рабочая копия убрана, 62 теста зелёные на основной ветке, сквозной смоук пройден фактически (создание, два перехода = clicks 2, HEAD = 302 без инкремента, демо-данные на английском), безопасность проверена (запросы параметризованы, схемы адреса по белому списку, защита от выхода за корень на разрешённом пути, тело ограничено 64 КБ), документы блока сверены с кодом. Задачи T004-T009, T013 закрыты
2026-07-30 15:26 [dispatcher] Приёмка поймала дефект, невидимый в копии агента: после слияния тесты упали, потому что .env лежит вне git и не переезжает — проект нельзя было собрать заново. Диспетчер собрал .env по документированным именам (62 теста зелёные) и добавил в репозиторий .env.example. Заведена T016 на HEAD для остальных маршрутов — агент честно отметил это как незакрытое, а не умолчал
2026-07-30 15:53 [dispatcher] Блок web ПРИНЯТ. Приёмка фактическая: агент не трогал ни общие файлы состояния, ни чужой блок src/ (проверено git log --right-only); markup-API не используется вовсе, только textContent (18 мест) — экранировать нечего по построению; слито, копия убрана; 96 тестов зелёные в основной копии; живой смоук — страница отдаётся (200, все элементы на месте), логика под /public/app.js, сквозной сценарий даёт clicks=2. Задачи T010-T012 закрыты
2026-07-30 15:53 [dispatcher] Найден собственный косяк: после смоука блока api остался жить сервер на порту 3100 (PID 18570) — мой pkill не совпал с реальной командной строкой из-за флага и молча ничего не убил, а я отчитался «сервер остановлен». Блок-агент web из-за этого работал на 3110 и потратил время на разбор чужого процесса. Убрано по PID; правило внесено в скилл
2026-07-30 15:56 [dispatcher] T016 закрыта диспетчером (рабочих копий блоков уже нет, правка на две строки): HEAD приведён к RFC на всех GET-маршрутах. Тест написан до правки и падал на трёх маршрутах, после правки зелёный. Всего 99 тестов, 0 падений. По ходу сломал файл неверной вырезкой (искал конец фрагмента с начала файла, а не после начала) — откатил и переделал точно, а не подгонял
2026-07-30 16:08 [converge] Итерация 1 из 3, свежим взглядом (агент, который не строил). Все 10 пунктов DoD MVP закрыты фактическим запуском: 302 не кешируется (3 навигации в браузере = 3 клика), счётчик честен под нагрузкой (30 параллельных переходов = +30, потерянных апдейтов нет), HEAD не накручивает, 503 на мёртвой базе проверен отдельным инстансом, сид сходится, XSS-вектор не сработал. 99 тестов, покрытие 99.23%
2026-07-30 16:08 [converge] НАЙДЕНО РАСХОЖДЕНИЕ (1): must-история «видеть, сколько раз по ссылке перешли» (spec.md, раздел user stories) работает только внутри одной сессии страницы. После перезагрузки счётчик недостижим: ничего не сохраняется, дедупа адресов нет, повторная отправка того же адреса даёт новый код. Заведена T017
2026-07-30 16:29 [dispatcher] T017 принята. Приёмка фактическая: ноль коммитов агента в общие файлы и в чужой блок, markup-API не появился, в хранилище только code и shortUrl, 120 тестов зелёные в основной копии (было 99), покрытие app.js 100% строк. Агент сам нашёл дыру в своём тестовом стенде: фальшивый элемент рождался видимым, тогда как разметка отдаёт блок результата скрытым — то есть утверждение «страница ничего не показала» было недоказуемо. Добавлен тест, сверяющий стенд с разметкой
2026-07-30 16:29 [dispatcher] Рабочая база вычищена от смоук-ссылок, сид приведён к эталону: только 4 демо-ссылки
2026-07-30 16:40 [converge] Итерация 2 из 3: расхождение первой итерации ЗАКРЫТО фактически — все три случая проверены в живом браузере с чтением хранилища (обычная перезагрузка, перезагрузка после двух переходов со свежим счётчиком от сервера, удалённая из базы ссылка → блок скрыт и запись очищена). Дополнительно проверено поведение при заблокированной сети: ссылка остаётся, запись не стирается. Все 10 пунктов DoD перепроверены, регрессий от T017 нет. Новых расхождений нет; три замечания уровня LOW внесены в spec.md как послеMVP-ные
2026-07-30 16:53 [dispatcher] Продукт запущен и проверен на тестовой площадке local: страница отдаётся (200), healthz 200, создание ссылки даёт 201 с кодом, два перехода дают clicks=2, демо-данные на английском без регистрации (4 ссылки). После проверки сервер остановлен по слушателю порта и порт перепроверен: продукт не оставлен работающим, потому что смотреть его сейчас некому. Поднять заново: npm run migrate:up && npm run seed:demo && PORT=3100 PUBLIC_BASE_URL=http://localhost:3100 npm start
2026-07-30 16:53 [converge] СВЕРКА СО СПЕКОЙ ЗАВЕРШЕНА. Итерация 2 расхождений не нашла, поэтому третья не проводится: лимит converge_limit=3 — это потолок, а не норма. Задачи на итерацию 3 в графе нет и не должно быть
2026-07-30 16:53 [dispatcher] Состояние проекта на конец прогона: продукт собран и проверен, сервер не запущен, база содержит только 4 демо-ссылки на эталонных значениях, git локальный без remote (репозитория на GitHub нет, потому что гейт не проходился — см. D009), беклог живёт в tasks.md
2026-07-31 00:20 [dispatcher] Разбор прогона написан (docs/forge/retro.md) по фактам журнала, а не по памяти. Пятнадцать дефектов системы правились сразу по ходу и уже в коммитах; в беклог системы ушло только открытое — 4 задачи (#12 шаблон брифа блок-агенту, #13 правило уборки данных, #14 две правки секции E анкеты, #15 штатный обход занятого браузера). Адрес беклога определён из remote репозитория системы, существующие задачи прочитаны — дубликатов нет. ПРОГОН ЗАВЕРШЁН
