<!-- managed by forge: append-only, формат строки ниже не менять — файл парсят forge-build и forge-status -->
# Журнал стройки

<!-- формат строки: "YYYY-MM-DD HH:MM [роль] событие". роль: setup (подготовка проекта: анкета, исследование, брейншторм, документы) | dispatcher | block:<имя> (агент блока) | converge | deploy (раскатка на целевую площадку). Новые записи добавляются в конец, старые не редактируются и не удаляются. Запуск любого агента записывается строкой вида «Запущен блок <имя>: роль <тип агента>, модель <модель>» — без неё не видно, какой ролью и какой моделью шла работа. Требование включается строкой-маркером «Формат журнала: с этой строки каждый запуск агента записывается с ролью и моделью»: диспетчер пишет её первым делом, а проверка целостности спрашивает записи только про блоки после маркера — журналы, которые велись до правила, оно не задевает. -->

<!-- 2026-07-29 10:00 [dispatcher] Пример записи: стройка запущена -->
