# Версии закреплены точно: канал должен подниматься одинаково через полгода.
# Проверены по PyPI 31.07.2026 — не по памяти: с первого раза я перепутал все три.
# aiogram требует Python >=3.10 (в образе 3.12), aiohttp >=3.10, asyncpg >=3.9.
#
# aiohttp приходит вместе с aiogram, но мы держим на нём HTTP-интерфейс канала,
# поэтому фиксируем явно: иначе обновление aiogram однажды подменит нам веб-слой.
aiogram==3.30.0
aiohttp==3.14.3
asyncpg==0.31.0
